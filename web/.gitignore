# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# web/dist is gitignored (also via the root .gitignore): it's the built bundle
# the Go binary embeds via //go:embed all:web/dist. Run `mise run build` to
# regenerate it locally; CI builds it for releases.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
